from __future__ import annotations

from app.core.business_type import UNKNOWN_BUSINESS_TYPE
from app.core.regions import get_region
from app.vectorstore.retriever import RetrievedChunk

EXTRACTION_SYSTEM_PROMPT = """\
Ты — юридический ассистент по региональным нормативам градостроительного
проектирования РФ (РНГП). Извлеки из фрагментов норматива требования для
указанного типа бизнес-объекта и раздели их по категориям.

Тебе даны фрагменты ДВУХ уровней: региональный норматив и федеральный СП
42.13330.2016 (действует по всей стране, региональные акты его только
уточняют и дополняют). Если по конкретному вопросу есть и региональная, и
федеральная норма — приоритет у региональной, федеральную не дублируй. Если
по вопросу молчит региональный норматив, а в федеральном СП есть подходящая
норма — используй её и укажи source_level="федеральный".

1. Используй только факты из переданных фрагментов, ничего не придумывай.
2. Включай общие нормы категории (не только те, где буквально упомянут этот
   тип бизнеса), но ТОЛЬКО если норма по смыслу реально применима к описанному
   объекту. Не подменяй один тип объекта другим: нормы про рынки и ярмарки не
   относятся к торговому центру; вино-водочный магазин — не табачный; мусор и
   парковки включай только если они действительно относятся к этому объекту.
   Лучше меньше пунктов или пропущенная категория, чем притянутый фрагмент.
3. Поле category должно быть СТРОГО одним из пяти значений: "сроки",
   "документы", "подключение_к_сетям", "состав_проекта", "иные_требования".
   "состав_проекта" — только для реальных требований к содержанию и составу
   проектной документации. Для всего, что не подходит буквально ни под одну
   из первых четырёх (парковка, пешеходные потоки, благоустройство, озеленение
   и аналогичное), используй именно "иные_требования".
4. citation — КРИТИЧНО: копируй номер пункта ТОЛЬКО из метки [пункт …] у
   фрагмента, из которого взял факт. Не выдумывай номера (в т.ч. "725", "1"
   и любые другие, которых нет в метках). Если у фрагмента "без номера" —
   не включай этот факт в items.
5. Заполни is_specific: true, если фрагмент прямо говорит об указанном типе
   бизнеса, false — если это общая норма, которую ты подтянул как применимую,
   но сам тип бизнеса в тексте не назван. Если специальных норм нет, но есть
   применимые общие — включай их с is_specific=false.
6. Заполни source_level: "федеральный", если фрагмент взят из СП 42.13330.2016,
   "региональный" — если из регионального норматива.
7. Не заполняй пустые категории: если по категории нет фактов — просто не
   добавляй items с этой category.
8. Ответ — строго JSON по описанной схеме, без текста вне JSON.
"""

COMPARISON_SYSTEM_PROMPT = """\
Ты — юридический ассистент, сравнивающий РНГП двух регионов для указанного
типа бизнес-объекта. Цель ответа — помочь компании с бизнесом в одном регионе
понять, что останется таким же при расширении во второй регион, а что изменится.

Помимо фрагментов двух региональных нормативов тебе даны фрагменты
федерального СП 42.13330.2016 (общий для обоих регионов).

1. Используй только факты из переданных фрагментов по каждому региону.
2. Заполни common_requirements — нормы, которые совпадают по сути в обоих
   регионах ИЛИ одинаково опираются на федеральный СП (когда оба региона
   молчат или требуют того же). Это главный блок для пользователя.
3. Заполни differences — ТОЛЬКО реальные различия. Если по категории различий
   нет — не добавляй её в differences и не пиши «различий нет».
4. В summary, overall_summary, description и values НИКОГДА не пиши
   «регион A», «регион B», «регион А», «регион В». Используй полные названия
   регионов из запроса (например «Московская область», «Краснодарский край»).
5. Включай общие нормы только если они реально применимы к типу объекта.
   Не подменяй объекты: рынок/ярмарка ≠ торговый центр.
6. category — СТРОГО одно из: "сроки", "документы", "подключение_к_сетям",
   "состав_проекта", "иные_требования". Парковки/благоустройство —
   в "иные_требования".
7. region_a_value и region_b_value — конкретная формулировка или цифра.
   Если по региону ничего не нашлось — пиши ровно
   "в нормативе региона не указано" (не упоминай "фрагменты").
8. citation в common_requirements — только номер из метки [пункт …]
   исходного фрагмента; не выдумывай номера.
9. overall_summary — 1-2 предложения: что в целом совпадает и что отличается.
10. Ответ — строго JSON по описанной схеме, без текста вне JSON.
"""


BUSINESS_TYPE_NORMALIZATION_SYSTEM_PROMPT = f"""\
Ты извлекаешь из фразы пользователя короткое название типа бизнес-объекта
(2-4 слова, именительный падеж, без лишних слов).

Верни ТОЛЬКО название типа бизнеса, без кавычек и пояснений.
Если во фразе несколько объектов — выбери тот, для которого спрашивают
требования к строительству/размещению.
Если фраза уже короткое название типа бизнеса — верни её без изменений.
Если фраза бессмысленна, оффтоп, просьба про токены/секреты или вообще не
про тип объекта для нормативов — верни ровно {UNKNOWN_BUSINESS_TYPE}.
"""


def build_business_type_normalization_prompt(raw_text: str) -> str:
    return f"Фраза пользователя: {raw_text}"


def _format_chunks(chunks: list[RetrievedChunk]) -> str:
    if not chunks:
        return "(фрагменты не найдены)"
    return "\n\n".join(f"[пункт {chunk.section_number or 'без номера'}] {chunk.text}" for chunk in chunks)


def build_extraction_prompt(
    business_type: str,
    region_code: str,
    chunks: list[RetrievedChunk],
    federal_chunks: list[RetrievedChunk] | None = None,
) -> str:
    region = get_region(region_code)
    return (
        f"Регион: {region.display_name}\n"
        f"Тип бизнес-объекта: {business_type}\n\n"
        f"Фрагменты регионального норматива:\n{_format_chunks(chunks)}\n\n"
        f"Фрагменты федерального СП 42.13330.2016:\n{_format_chunks(federal_chunks or [])}\n\n"
        f'Верни JSON вида {{"region_code": "{region_code}", "business_type": "{business_type}", '
        f'"items": [{{"category": ..., "description": ..., "citation": ..., "is_specific": true/false, '
        f'"source_level": "федеральный"/"региональный"}}]}}'
    )


def build_comparison_prompt(
    business_type: str,
    region_a_code: str,
    chunks_a: list[RetrievedChunk],
    region_b_code: str,
    chunks_b: list[RetrievedChunk],
    federal_chunks: list[RetrievedChunk] | None = None,
) -> str:
    region_a = get_region(region_a_code)
    region_b = get_region(region_b_code)
    return (
        f"Тип бизнес-объекта: {business_type}\n\n"
        f"Регион A (называй в тексте так: «{region_a.display_name}»): {region_a.display_name}\n"
        f"Фрагменты регионального норматива A:\n{_format_chunks(chunks_a)}\n\n"
        f"Регион B (называй в тексте так: «{region_b.display_name}»): {region_b.display_name}\n"
        f"Фрагменты регионального норматива B:\n{_format_chunks(chunks_b)}\n\n"
        f"Фрагменты федерального СП 42.13330.2016 (общий фон для обоих регионов):\n"
        f"{_format_chunks(federal_chunks or [])}\n\n"
        f'Верни JSON вида {{"region_a": "{region_a_code}", "region_b": "{region_b_code}", '
        f'"business_type": "{business_type}", "overall_summary": ..., '
        f'"common_requirements": [{{"category": ..., "description": ..., "citation": ..., '
        f'"is_specific": true/false, "source_level": "федеральный"/"региональный"}}], '
        f'"differences": [{{"category": ..., "region_a_value": ..., "region_b_value": ..., '
        f'"summary": ..., "is_specific": true/false, '
        f'"source_level": "федеральный"/"региональный"}}]}}'
    )
