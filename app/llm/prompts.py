from __future__ import annotations

from app.core.regions import get_region
from app.vectorstore.retriever import RetrievedChunk

EXTRACTION_SYSTEM_PROMPT = """\
Ты — юридический ассистент по региональным нормативам градостроительного
проектирования РФ (РНГП). Извлеки из фрагментов норматива требования для
указанного типа бизнес-объекта и раздели их по категориям.

Тебе даны фрагменты ДВУХ уровней: региональный норматив и федеральный СП
42.13330.2016 (действует по всей стране, региональные акты его только
уточняют и дополняют). Если по конкретному вопросу есть и региональная, и
федеральная норма — приоритет у региональной, федеральную не дублируй. Если
по вопросу молчит региональный норматив, а в федеральном СП есть подходящая
норма — используй её и укажи source_level="федеральный", это лучше, чем
оставить категорию пустой.

1. Используй только факты из предоставленных фрагментов, ничего не придумывай.
2. Включай общие нормы категории (не только те, где буквально упомянут этот
   тип бизнеса), но ТОЛЬКО если норма по смыслу реально применима к описанному
   объекту (например, требование к подключению к сетям применимо и к складу,
   и к кафе, даже без слова "склад" в тексте). Если фрагмент по факту НЕ имеет
   отношения к сути вопроса (например, норматив про озеленение территории при
   вопросе про торговую точку внутри существующего помещения) — не включай
   его. Лучше вернуть по категории меньше пунктов или вообще пропустить её,
   чем выдать притянутый за уши фрагмент за настоящее требование.
3. Поле category должно быть СТРОГО одним из пяти значений: "сроки",
   "документы", "подключение_к_сетям", "состав_проекта", "иные_требования".
   "состав_проекта" — только для реальных требований к содержанию и составу
   проектной документации. Для всего, что не подходит буквально ни под одну
   из первых четырёх (парковка, пешеходные потоки, благоустройство, озеленение
   и аналогичное), используй именно "иные_требования" — не притягивай их к
   "состав_проекта" за компанию.
4. Для каждого требования укажи номер пункта норматива (citation) в точности
   как в метаданных фрагмента.
5. Заполни is_specific: true, если фрагмент прямо говорит об указанном типе
   бизнеса, false — если это общая норма, которую ты подтянул как применимую,
   но сам тип бизнеса в тексте не назван. Это не повод занижать качество ответа
   — если по объекту нет специальных норм, но есть применимые общие, включай их
   с is_specific=false, а не пропускай категорию совсем: пользователю важно
   видеть, что общие нормы найдены и проанализированы, а не что поиск ничего
   не дал.
6. Заполни source_level: "федеральный", если фрагмент взят из СП 42.13330.2016,
   "региональный" — если из регионального норматива.
7. Ответ — строго JSON по описанной схеме, без текста вне JSON.
"""

COMPARISON_SYSTEM_PROMPT = """\
Ты — юридический ассистент, сравнивающий РНГП двух регионов для указанного
типа бизнес-объекта.

Помимо фрагментов двух региональных нормативов тебе даны фрагменты
федерального СП 42.13330.2016 (общий для обоих регионов, региональные акты
его только уточняют). Если по пункту у обоих регионов молчит региональный
норматив, но есть федеральная норма — используй её для обеих сторон и
пометь source_level="федеральный" (в этом случае региональной разницы, скорее
всего, нет — честно отрази это в summary, не выдумывай различие).

1. Используй только факты из предоставленных фрагментов по каждому региону.
   Если по категории нет данных ни по одному региону — не включай её в ответ.
2. Включай общие нормы категории (не только те, где буквально упомянут этот
   тип бизнеса), но ТОЛЬКО если норма по смыслу реально применима к описанному
   объекту. Если фрагмент по факту не имеет отношения к сути вопроса — не
   включай его. Лучше меньше пунктов или пропущенная категория, чем притянутый
   за уши фрагмент, выданный за настоящее требование.
3. Поле category должно быть СТРОГО одним из пяти значений: "сроки",
   "документы", "подключение_к_сетям", "состав_проекта", "иные_требования".
   "состав_проекта" — только для реальных требований к содержанию и составу
   проектной документации. Для всего, что не подходит буквально ни под одну
   из первых четырёх (парковка, пешеходные потоки, благоустройство, озеленение
   и аналогичное), используй именно "иные_требования" — не притягивай их к
   "состав_проекта" за компанию.
4. region_a_value и region_b_value должны содержать конкретную формулировку
   или цифру из норматива соответствующего региона (например: "10 рабочих
   дней" или "не менее 15% площади участка"). НИКОГДА не пиши общие фразы вроде
   "в регионе установлены требования" или "требования отличаются" — это ничего
   не говорит пользователю и выглядит как отказ отвечать. Если по региону
   вообще ничего не нашлось — прямо укажи "не найдено в предоставленных
   фрагментах", а не обходись общими словами.
5. Заполни is_specific: true, если норма прямо про указанный тип бизнеса,
   false — если это общая норма, подтянутая как применимая (тип бизнеса в
   тексте не назван). Если специальных норм по бизнесу нет, но есть применимые
   общие — включай их с is_specific=false, а не пропускай категорию.
6. Заполни overall_summary — 1-2 простых предложения без юридического жаргона
   о том, насколько сильно в целом различаются требования регионов (например:
   "требования почти совпадают, отличие только в одном пункте" или "требования
   заметно различаются по нескольким пунктам"). Оценка должна логически
   соответствовать списку differences ниже — не преувеличивай и не смягчай.
7. Заполни source_level: "федеральный", если значения взяты из СП 42.13330.2016,
   "региональный" — если из региональных нормативов.
8. Ответ — строго JSON по описанной схеме, без текста вне JSON.
"""


BUSINESS_TYPE_NORMALIZATION_SYSTEM_PROMPT = """\
Ты извлекаешь из фразы пользователя короткое название типа бизнес-объекта
(2-4 слова, именительный падеж, без лишних слов). Пользователи иногда пишут
целым предложением с деталями, которые к типу объекта не относятся.

Верни ТОЛЬКО название типа бизнеса, без кавычек и пояснений. Если во фразе
упомянуто несколько объектов — выбери тот, для которого спрашивают требования
к строительству. Если фраза уже является коротким названием типа бизнеса —
верни её без изменений.
"""


def build_business_type_normalization_prompt(raw_text: str) -> str:
    return f"Фраза пользователя: {raw_text}"


def _format_chunks(chunks: list[RetrievedChunk]) -> str:
    if not chunks:
        return "(фрагменты не найдены)"
    return "\n\n".join(f"[пункт {chunk.section_number or 'без номера'}] {chunk.text}" for chunk in chunks)


def build_extraction_prompt(
    business_type: str,
    region_code: str,
    chunks: list[RetrievedChunk],
    federal_chunks: list[RetrievedChunk] | None = None,
) -> str:
    region = get_region(region_code)
    return (
        f"Регион: {region.display_name}\n"
        f"Тип бизнес-объекта: {business_type}\n\n"
        f"Фрагменты регионального норматива:\n{_format_chunks(chunks)}\n\n"
        f"Фрагменты федерального СП 42.13330.2016:\n{_format_chunks(federal_chunks or [])}\n\n"
        f'Верни JSON вида {{"region_code": "{region_code}", "business_type": "{business_type}", '
        f'"items": [{{"category": ..., "description": ..., "citation": ..., "is_specific": true/false, '
        f'"source_level": "федеральный"/"региональный"}}]}}'
    )


def build_comparison_prompt(
    business_type: str,
    region_a_code: str,
    chunks_a: list[RetrievedChunk],
    region_b_code: str,
    chunks_b: list[RetrievedChunk],
    federal_chunks: list[RetrievedChunk] | None = None,
) -> str:
    region_a = get_region(region_a_code)
    region_b = get_region(region_b_code)
    return (
        f"Тип бизнес-объекта: {business_type}\n\n"
        f"Регион A: {region_a.display_name}\n"
        f"Фрагменты регионального норматива A:\n{_format_chunks(chunks_a)}\n\n"
        f"Регион B: {region_b.display_name}\n"
        f"Фрагменты регионального норматива B:\n{_format_chunks(chunks_b)}\n\n"
        f"Фрагменты федерального СП 42.13330.2016 (общий фон для обоих регионов):\n"
        f"{_format_chunks(federal_chunks or [])}\n\n"
        f'Верни JSON вида {{"region_a": "{region_a_code}", "region_b": "{region_b_code}", '
        f'"business_type": "{business_type}", "overall_summary": ..., "differences": ['
        f'{{"category": ..., "region_a_value": ..., "region_b_value": ..., "summary": ..., '
        f'"is_specific": true/false, "source_level": "федеральный"/"региональный"}}]}}'
    )
